# Solar Lead Scraper - Environment Variables
# Copy this file to .env and fill in your values

# Required for dashboard upload
DASHBOARD_URL=https://your-ops-dashboard.vercel.app
DASHBOARD_API_KEY=your-api-key-here

# Optional: Batch mode (used by GitHub Actions for parallel scraping)
# SCRAPER_BATCH=0
# SCRAPER_TOTAL_BATCHES=4

# Optional: Proxy support (comma-separated list)
# Format: "user:pass@host:port" or "host:port"
# SCRAPER_PROXIES=proxy1.example.com:8080,user:pass@proxy2.example.com:8080

# Optional: Browser-based scraping for sites that block python-jobspy
# Set to "1" to enable Camoufox browser scraping (requires camoufox install)
# ENABLE_BROWSER_SCRAPING=1
